name: Run all validations

on:
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build application
    runs-on: ubuntu-latest
    needs: checkout

    steps:
      - name: Checkout code
        uses: ../actions/install_dependencies.yml

      - name: 🚀 Build Remix App
        run: yarn build
